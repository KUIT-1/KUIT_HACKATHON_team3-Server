package kuit.server.repository;

import kuit.server.domain.Detail_Category;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Detail_CategoryRepository extends JpaRepository<Detail_Category, Long> {
}